import { mutarise } from '../../src'

describe('transformations: mutarise', () => {
  test('calculates new column with mean without by as expected', () => {

  })

  test('overwrites column with mean without by as expected ', () => {

  })
})
